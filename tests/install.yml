---
- hosts: localhost
  connection: local
  roles:
    - lae.travis-lxc
  vars:
    test_profiles:
      - profile: debian-stretch
      - profile: ubuntu-xenial
    lxc_use_overlayfs: no

- hosts: awx
  become: True
  roles:
    - lae.docker
